import React from "react";

const Header = () => {

    return (
        <main className="container_header">
            <h2>Cálculo de quantidade de Tinta</h2>
        </main>
    );
};

export default Header;